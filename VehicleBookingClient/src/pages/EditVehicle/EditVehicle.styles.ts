import { styled } from "@mui/material/styles";
import { Box, Typography } from "@mui/material";

export const PageContainer = styled(Box)({ padding: 30, maxWidth: 600 });
export const PageTitle = styled(Typography)({ marginBottom: 20, fontWeight: 700 });